using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoomTriggerController : MonoBehaviour
{
    [SerializeField] private GameObject actualRoom;
    [SerializeField] private GameObject nextRoom;

    // Hacer movidas de llamar a un controlador que mueva la camara con el lerp
}
